services:
  nestor:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8790:8790"
    volumes:
      - ./config.yaml:/etc/nestor/config.yaml:ro
    healthcheck:
      test: wget -qO- http://127.0.0.1:8790/healthz || exit 1
      interval: 30s
      timeout: 3s
      retries: 3
